<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select Settings to Import/Export"
        Width="440"
        SizeToContent="Height"
        MaxHeight="501"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="False"
        ShowInTaskbar="False">

    <Border BorderBrush="{DynamicResource BorderColor}"
            BorderThickness="1"
            CornerRadius="12"
            Background="{DynamicResource CardBgColor}"
            Margin="25">
        <Border.Effect>
            <DropShadowEffect Color="Black"
                              Opacity="0.3"
                              BlurRadius="30"
                              ShadowDepth="0"
                              Direction="0"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Title Bar -->
            <Grid Grid.Row="0" x:Name="TitleBar" Height="40" Background="Transparent">
                <TextBlock x:Name="TitleText"
                           Foreground="{DynamicResource FgColor}"
                           FontSize="18"
                           FontWeight="SemiBold"
                           VerticalAlignment="Center"
                           Margin="24,12,0,0"/>
            </Grid>

            <!-- Content -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="0,0,0,0">
                <StackPanel x:Name="ContentPanel" Margin="24,12,24,24">
                    <TextBlock x:Name="PromptText"
                               TextWrapping="Wrap"
                               FontSize="14"
                               LineHeight="22"
                               Opacity="0.85"
                               Foreground="{DynamicResource FgColor}"
                               Margin="0,0,0,16"/>
                    <!-- Checkboxes are added dynamically at runtime -->
                    <StackPanel x:Name="CheckboxPanel"/>
                </StackPanel>
            </ScrollViewer>

            <!-- Button Footer -->
            <Border Grid.Row="2"
                    Background="{DynamicResource BgColor}"
                    BorderBrush="{DynamicResource BorderColor}"
                    BorderThickness="0,1,0,0"
                    Padding="16,16"
                    CornerRadius="0,0,12,12">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="CancelButton"
                            Content="Cancel"
                            Height="36" MinWidth="90" Margin="8,0,0,0"
                            Style="{DynamicResource SecondaryButtonStyle}"/>
                    <Button x:Name="OkButton"
                            Content="OK"
                            Height="36" MinWidth="90" Margin="8,0,0,0"
                            Style="{DynamicResource PrimaryButtonStyle}"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
