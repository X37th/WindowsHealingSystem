<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Windows Healing System" 
        MinWidth="1130" MinHeight="600"
        ResizeMode="CanResize" 
        SnapsToDevicePixels="True"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="False"
        Background="{DynamicResource BgColor}"
        Foreground="{DynamicResource FgColor}">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome ResizeBorderThickness="0"
                            CaptionHeight="40"
                            CornerRadius="12"
                            GlassFrameThickness="-1"
                            UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>
    <Window.Resources>

        <!-- ComboBox Style -->
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource FgColor}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="0,6,0,16"/>
            <Setter Property="MinHeight" Value="36"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <!-- Left accent line -->
                            <Border x:Name="ClosedAccentLine" Width="3" Height="18" HorizontalAlignment="Left" VerticalAlignment="Stretch" Background="{DynamicResource SelectionAccentColor}" CornerRadius="1.5" Panel.ZIndex="2"/>
                            <ToggleButton x:Name="ToggleButton" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Focusable="False" IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" ClickMode="Press">
                                <ToggleButton.Style>
                                    <Style TargetType="ToggleButton">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="ToggleButton">
                                                    <Border x:Name="Border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6">
                                                        <TextBlock x:Name="Arrow"
                                                                Text="&#xE70D;"
                                                                FontFamily="Segoe Fluent Icons"
                                                                FontSize="11"
                                                                HorizontalAlignment="Right"
                                                                VerticalAlignment="Center"
                                                                Margin="0,0,12,0"
                                                                Foreground="{DynamicResource FgColor}"
                                                                RenderTransformOrigin="0.5,0.5">
                                                            <TextBlock.RenderTransform>
                                                                <RotateTransform x:Name="ArrowRotation" Angle="0"/>
                                                            </TextBlock.RenderTransform>
                                                        </TextBlock>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter TargetName="Border" Property="Background" Value="{DynamicResource ComboHoverColor}"/>
                                                        </Trigger>
                                                        <Trigger Property="IsChecked" Value="True">
                                                            <Trigger.EnterActions>
                                                                <BeginStoryboard>
                                                                    <Storyboard>
                                                                        <DoubleAnimation Storyboard.TargetName="Arrow" Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)" To="180" Duration="0:0:0.2">
                                                                            <DoubleAnimation.EasingFunction>
                                                                                <CubicEase EasingMode="EaseOut"/>
                                                                            </DoubleAnimation.EasingFunction>
                                                                        </DoubleAnimation>
                                                                    </Storyboard>
                                                                </BeginStoryboard>
                                                            </Trigger.EnterActions>
                                                            <Trigger.ExitActions>
                                                                <BeginStoryboard>
                                                                    <Storyboard>
                                                                        <DoubleAnimation Storyboard.TargetName="Arrow" Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)" To="0" Duration="0:0:0.2">
                                                                            <DoubleAnimation.EasingFunction>
                                                                                <CubicEase EasingMode="EaseOut"/>
                                                                            </DoubleAnimation.EasingFunction>
                                                                        </DoubleAnimation>
                                                                    </Storyboard>
                                                                </BeginStoryboard>
                                                            </Trigger.ExitActions>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </ToggleButton.Style>
                            </ToggleButton>
                            <ContentPresenter x:Name="ContentPresenter"
                                            IsHitTestVisible="False"
                                            Margin="12,0,24,0"
                                            HorizontalAlignment="Left"
                                            VerticalAlignment="Center"
                                            Content="{TemplateBinding SelectionBoxItem}"
                                            ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                            ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"/>
                            <Popup x:Name="Popup"
                                Placement="Bottom"
                                IsOpen="{TemplateBinding IsDropDownOpen}"
                                AllowsTransparency="True"
                                Focusable="False"
                                PopupAnimation="Fade"
                                StaysOpen="False"
                                PlacementTarget="{Binding ElementName=ToggleButton}"
                                VerticalOffset="1"
                                HorizontalOffset="0">
                                <Grid x:Name="DropDown" MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}" Margin="12">
                                    <Border x:Name="DropDownBorder"
                                            Background="{DynamicResource ComboItemBgColor}"
                                            BorderBrush="{DynamicResource BorderColor}"
                                            BorderThickness="1"
                                            CornerRadius="6"
                                            Padding="4">
                                        <Border.Effect>
                                            <DropShadowEffect BlurRadius="12" Opacity="0.1" ShadowDepth="2" Direction="270"/>
                                        </Border.Effect>
                                        <ScrollViewer Margin="0,2,0,0"
                                                    VerticalScrollBarVisibility="Auto"
                                                    HorizontalScrollBarVisibility="Disabled">
                                            <ItemsPresenter Margin="0,0,0,1"/>
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="SelectedIndex" Value="0">
                                <Setter TargetName="ClosedAccentLine" Property="Visibility" Value="Collapsed"/>
                            </Trigger>
                            <Trigger Property="SelectedIndex" Value="-1">
                                <Setter TargetName="ClosedAccentLine" Property="Visibility" Value="Collapsed"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ClosedAccentLine" Property="Visibility" Value="Collapsed"/>
                                <Setter Property="Background" Value="{DynamicResource ButtonDisabled}"/>
                                <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
                                <Setter Property="Foreground" Value="{DynamicResource ButtonTextDisabled}"/>
                                <Setter Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBoxItem Style -->
        <Style TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="{DynamicResource FgColor}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Margin" Value="0,0,0,2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Grid>
                            <Border x:Name="ItemBorder"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    Padding="{TemplateBinding Padding}"
                                    CornerRadius="4">
                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                            </Border>
                            <!-- Left accent line -->
                            <Border x:Name="AccentLine"
                                    Width="3"
                                    Height="15"
                                    HorizontalAlignment="Left"
                                    VerticalAlignment="Stretch"
                                    Background="{DynamicResource SelectionAccentColor}"
                                    CornerRadius="1.5"
                                    Margin="0"
                                    Visibility="Collapsed"/>
                        </Grid>
                        <ControlTemplate.Triggers> 
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="AccentLine" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource ComboItemSelectedColor}"/>
                            </Trigger>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource ComboItemHoverColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- TextBlock Label Style -->
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource FgColor}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Margin" Value="0,0,0,0"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <!-- Label Border Style for ComboBox labels -->
        <Style x:Key="LabelBorderStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Padding" Value="4,4"/>
            <Setter Property="Margin" Value="0,0,0,0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
        </Style>

        <Style x:Key="SegmentedGroupContainerStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource ContentBgColor}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="14,12,14,12"/>
            <Setter Property="Margin" Value="0,2,0,4"/>
        </Style>

        <!-- Subsection header label for multi-option controls inside a category card -->
        <Style x:Key="MultiOptionSectionHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="10.5"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource FgColor}"/>
            <Setter Property="Opacity" Value="0.45"/>
            <Setter Property="Margin" Value="0,10,0,4"/>
        </Style>

        <Style x:Key="GhostButtonStyle" TargetType="ButtonBase">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource FgColor}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ButtonBase">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ComboHoverColor}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ComboItemSelectedColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Shadcn-style Flat Category Card -> Minimal Flat List -->
        <Style x:Key="CategoryCardBorderStyle" TargetType="Border">
            <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="CornerRadius" Value="0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="4,8,4,16"/>
            <Setter Property="Margin" Value="0,0,0,16"/>
        </Style>

        <!-- Shadcn-style Category Header -->
        <Style x:Key="CategoryHeaderTextBlock" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="17"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
            <Setter Property="Foreground" Value="{DynamicResource HeroTitleColor}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <!-- Category header icon style -->
        <Style x:Key="CategoryHeaderIcon" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe Fluent Icons"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="Foreground" Value="{DynamicResource FgColor}"/>
            <Setter Property="Margin" Value="0,0,12,20"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="MenuItemCheckBoxStyle" TargetType="CheckBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource FgColor}"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="4">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="32"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <!-- The Tick Mark / Indeterminate Mark -->
                                <TextBlock x:Name="Tick" Text="&#xE73E;" FontFamily="Segoe Fluent Icons" FontSize="14" Foreground="{DynamicResource FgColor}" VerticalAlignment="Center" HorizontalAlignment="Center" Visibility="Hidden"/>
                                <!-- The Content -->
                                <ContentPresenter Grid.Column="1" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Tick" Property="Visibility" Value="Visible"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="{x:Null}">
                                <Setter TargetName="Tick" Property="Text" Value="&#xE738;"/>
                                <Setter TargetName="Tick" Property="Visibility" Value="Visible"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ComboHoverColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>


        <!-- Chip-style CheckBox for inline preset selection strips -->
        <Style x:Key="ChipCheckBoxStyle" TargetType="CheckBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource FgColor}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="16">
                            <StackPanel Orientation="Horizontal" Margin="{TemplateBinding Padding}">
                                <TextBlock x:Name="Tick" Text="&#xE73E;" FontFamily="Segoe Fluent Icons" FontSize="12" Foreground="{DynamicResource FgColor}" VerticalAlignment="Center" Margin="0,0,6,0" Visibility="Collapsed"/>
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Tick" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ComboItemSelectedColor}"/>
                                <Setter TargetName="Bd" Property="BorderBrush" Value="Transparent"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="{x:Null}">
                                <Setter TargetName="Tick" Property="Text" Value="&#xE738;"/>
                                <Setter TargetName="Tick" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ComboHoverColor}"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ComboHoverColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Category help link button/text styles -->

        <Style x:Key="CategoryHelpLinkButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="6,1,0,13"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="IsTabStop" Value="False"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Foreground" Value="{DynamicResource SelectionAccentColor}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="Transparent" BorderBrush="Transparent" BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="{DynamicResource ButtonHover}"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Foreground" Value="{DynamicResource ButtonPressed}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CategoryHelpLinkTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Foreground">
                <Setter.Value>
                    <Binding RelativeSource="{RelativeSource AncestorType=Button}" Path="Foreground"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Overview changes text style -->
        <Style x:Key="OverviewNoChangesTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource FgColor}"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Opacity" Value="0.6"/>
        </Style>

        <Style x:Key="OverviewChangeBulletStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource FgColor}"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Opacity" Value="0.85"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>

        <!-- TextBox Style for search -->
        <Style TargetType="TextBox">
            <Setter Property="CaretBrush" Value="{DynamicResource FgColor}"/>
        </Style>

        <!-- User TextBox Style with disabled state -->
        <Style x:Key="UserTextBoxStyle" TargetType="TextBox">
            <Setter Property="CaretBrush" Value="{DynamicResource FgColor}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource FgColor}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Margin" Value="1,0,0,1"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Padding" Value="0"/>
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="{DynamicResource ButtonDisabled}"/>
                    <Setter Property="Foreground" Value="{DynamicResource ButtonTextDisabled}"/>
                    <Setter Property="Opacity" Value="0.6"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- User TextBox Border Style with disabled state -->
        <Style x:Key="UserTextBoxBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource ComboBgColor}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource ButtonBorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Padding" Value="12,8"/>
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="{DynamicResource ButtonDisabled}"/>
                    <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
                    <Setter Property="Opacity" Value="0.6"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource ComboHoverColor}"/>
                </Trigger>
            </Style.Triggers>
        </Style>


        <!-- TextBlock style for App Name column in apps table -->
        <Style x:Key="AppNameTextStyle" TargetType="TextBlock">
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <!-- TextBlock style for Description column in apps table -->
        <Style x:Key="AppDescTextStyle" TargetType="TextBlock">
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="16,0,16,0"/>
            <Setter Property="Opacity" Value="0.7"/>
        </Style>

        <!-- Recommendation dot shape style for app matrix cards -->
        <Style x:Key="AppRecommendationDotStyle" TargetType="Ellipse">
            <Setter Property="Width" Value="8"/>
            <Setter Property="Height" Value="8"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <!-- Validation feedback colors for username input -->
        <SolidColorBrush x:Key="ValidationErrorColor" Color="#c42b1c"/>
        <SolidColorBrush x:Key="ValidationSuccessColor" Color="#28a745"/>

        <!-- Title Bar Button Style -->
        <Style x:Key="TitleBarButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource FgColor}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="46"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="FontFamily" Value="Segoe Fluent Icons"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderThickness="0" CornerRadius="0,8,0,0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="CloseButton" TargetType="Button" BasedOn="{StaticResource TitleBarButton}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource CloseHover}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="TitlebarButton" TargetType="Button" BasedOn="{StaticResource TitleBarButton}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderThickness="0" CornerRadius="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource TitlebarButtonHover}"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="{DynamicResource TitlebarButtonPressed}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- SearchBox Border Style -->
        <Style x:Key="SearchBoxBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource ComboBgColor}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource ButtonBorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Width" Value="300"/>
            <Setter Property="Padding" Value="12,8"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- ContextMenu Style -->
        <Style TargetType="ContextMenu">
            <Setter Property="Background" Value="{DynamicResource CardBgColor}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="4"/>
            <Setter Property="HasDropShadow" Value="True"/>
            <Setter Property="HorizontalOffset" Value="-12"/>
            <Setter Property="VerticalOffset" Value="-12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ContextMenu">
                        <Border Margin="12">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="8"
                                    Padding="4">
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="16" Opacity="0.15" ShadowDepth="4" Direction="270"/>
                                </Border.Effect>
                                <StackPanel IsItemsHost="True"/>
                            </Border>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- MenuItem Style -->
        <Style TargetType="MenuItem">
            <Setter Property="Foreground" Value="{DynamicResource FgColor}"/>
            <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Padding" Value="12,8"/>
        <Setter Property="Height" Value="36"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Border Background="{TemplateBinding Background}"
                                BorderThickness="0"
                            CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="24"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <ContentPresenter Grid.Column="0" ContentSource="Icon" 
                                                VerticalAlignment="Center"
                                                HorizontalAlignment="Center"/>
                                <ContentPresenter Grid.Column="1" ContentSource="Header" 
                                                VerticalAlignment="Center"
                                                Margin="8,0,0,1"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter Property="Background" Value="{DynamicResource ComboBgColor}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{DynamicResource ComboHoverColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Shadcn-style Sidebar Navigation Button -->
        <Style x:Key="SidebarNavStyle" TargetType="RadioButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource FgColor}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Margin" Value="0,4"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource ComboHoverColor}"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Background" Value="{DynamicResource ComboItemSelectedColor}"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SegmentedButtonStyle" TargetType="RadioButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource FgColor}"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource ComboHoverColor}"/>
                </Trigger>
                <!-- Active view: white/neutral â€” no colored fill -->
                <Trigger Property="IsChecked" Value="True">
                    <Setter Property="Background" Value="{DynamicResource ComboItemSelectedColor}"/>
                    <Setter Property="Foreground" Value="{DynamicResource FgColor}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- OptionChipStyle, PresetChipStyle, and FeatureCheckboxStyle have been moved and centralized in SharedStyles.xaml -->

        <!-- PresetCheckBoxStyle: defined in SharedStyles.xaml (standard checkbox appearance).
             Removed local override here that was BasedOn FeatureCheckboxStyle (toggle switch)
             which gave Quick Select dropdown items incorrect toggle-switch UI. -->

        <!-- Sidebar Tools Buttons (Import/Export/Docs) Ã¢â‚¬â€ DynamicResource hover, theme-aware -->
        <Style x:Key="SidebarToolsButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="{DynamicResource SidebarLabelColor}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="Margin" Value="0,4"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="BtnBorder" Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="BtnBorder" Property="Background" Value="{DynamicResource ComboHoverColor}"/>
                                <Setter Property="Foreground" Value="{DynamicResource FgColor}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="BtnBorder" Property="Background" Value="{DynamicResource ComboItemSelectedColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border x:Name="MainBorder" BorderBrush="{DynamicResource BorderColor}" 
            BorderThickness="1" 
            CornerRadius="12" 
            Background="{DynamicResource BgColor}"
            Margin="0">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Resize Borders -->
            <Rectangle x:Name="ResizeLeft" Width="5" HorizontalAlignment="Left" Fill="Transparent" Cursor="SizeWE" Grid.Row="0" Grid.RowSpan="2" Panel.ZIndex="100" Visibility="Collapsed" IsHitTestVisible="False"/>
            <Rectangle x:Name="ResizeRight" Width="5" HorizontalAlignment="Right" Fill="Transparent" Cursor="SizeWE" Grid.Row="0" Grid.RowSpan="2" Panel.ZIndex="100" Visibility="Collapsed" IsHitTestVisible="False"/>
            <Rectangle x:Name="ResizeTop" Height="5" VerticalAlignment="Top" Fill="Transparent" Cursor="SizeNS" Grid.Row="0" Panel.ZIndex="100" Visibility="Collapsed" IsHitTestVisible="False"/>
            <Rectangle x:Name="ResizeBottom" Height="5" VerticalAlignment="Bottom" Fill="Transparent" Cursor="SizeNS" Grid.Row="1" Panel.ZIndex="100" Visibility="Collapsed" IsHitTestVisible="False"/>
            <Rectangle x:Name="ResizeTopLeft" Width="8" Height="8" HorizontalAlignment="Left" VerticalAlignment="Top" Fill="Transparent" Cursor="SizeNWSE" Grid.Row="0" Panel.ZIndex="101" Visibility="Collapsed" IsHitTestVisible="False"/>
            <Rectangle x:Name="ResizeTopRight" Width="8" Height="8" HorizontalAlignment="Right" VerticalAlignment="Top" Fill="Transparent" Cursor="SizeNESW" Grid.Row="0" Panel.ZIndex="101" Visibility="Collapsed" IsHitTestVisible="False"/>
            <Rectangle x:Name="ResizeBottomLeft" Width="8" Height="8" HorizontalAlignment="Left" VerticalAlignment="Bottom" Fill="Transparent" Cursor="SizeNESW" Grid.Row="1" Panel.ZIndex="101" Visibility="Collapsed" IsHitTestVisible="False"/>
            <Rectangle x:Name="ResizeBottomRight" Width="8" Height="8" HorizontalAlignment="Right" VerticalAlignment="Bottom" Fill="Transparent" Cursor="SizeNWSE" Grid.Row="1" Panel.ZIndex="101" Visibility="Collapsed" IsHitTestVisible="False"/>

            <!-- Custom Title Bar -->
            <Grid Grid.Row="0" x:Name="TitleBar">
                <Border x:Name="TitleBarBackground" Background="{DynamicResource BgColor}" CornerRadius="12,12,0,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Grid.ColumnSpan="2"
                                   Text="Windows Healing System" 
                                   Foreground="{DynamicResource FgColor}"
                                   FontWeight="Bold"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center" 
                                   IsHitTestVisible="False"
                                   FontSize="16"/>
                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                            <!-- Theme toggle: icon is Button Content (not in Template) so FindName works -->
                            <Button x:Name="ThemeToggleBtn" shell:WindowChrome.IsHitTestVisibleInChrome="True"
                                    Width="46" Height="30" Cursor="Hand"
                                    AutomationProperties.Name="Toggle Theme">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Background" Value="Transparent"/>
                                        <Setter Property="BorderThickness" Value="0"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="4" Padding="10,0">
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource TitlebarButtonHover}"/>
                                                        </Trigger>
                                                        <Trigger Property="IsPressed" Value="True">
                                                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource TitlebarButtonPressed}"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </Button.Style>
                                <!-- TextBlock as Content Ã¢â‚¬â€ lives in window NameScope, FindName works -->
                                <TextBlock x:Name="ThemeToggleIcon" Text="&#xF08C;"
                                           FontFamily="Segoe Fluent Icons" FontSize="13"
                                           Foreground="{DynamicResource FgColor}"
                                           ToolTip="Switch to light mode"/>
                            </Button>
                            <Button x:Name="CloseBtn" shell:WindowChrome.IsHitTestVisibleInChrome="True" Content="&#xE8BB;" Style="{StaticResource CloseButton}" ToolTip="Close" AutomationProperties.Name="Close"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>

            <!-- Main Content with Tabs -->
            <!-- Main Content Area (Replaces TabControl and BottomNavGrid) -->
            <Grid Grid.Row="1" x:Name="ContentGrid" Margin="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="210"/> <!-- Sidebar -->
                    <ColumnDefinition Width="*"/>   <!-- Main Content -->
                </Grid.ColumnDefinitions>

                <!-- Sidebar Navigation -->
                <Border Grid.Column="0" Width="210" Background="{DynamicResource SidebarBgColor}" BorderBrush="{DynamicResource SidebarBorderColor}" BorderThickness="0,0,1,0" Padding="8,32,8,24" Panel.ZIndex="5">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/> <!-- Navigation -->
                            <RowDefinition Height="*"/>    <!-- Spacer -->
                            <RowDefinition Height="Auto"/> <!-- Tools Section -->
                        </Grid.RowDefinitions>

                        <!-- Primary Navigation -->
                        <StackPanel Grid.Row="0">
                            <TextBlock Text="CONFIGURATION" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource SidebarLabelColor}" Margin="4,0,0,12"/>
                            <RadioButton x:Name="NavHomeBtn" GroupName="SidebarNav" Content="Home" IsChecked="True" Style="{DynamicResource SidebarNavStyle}"/>
                            <RadioButton x:Name="NavAppsBtn" GroupName="SidebarNav" Content="App Removal" Style="{DynamicResource SidebarNavStyle}"/>
                            <RadioButton x:Name="NavTweaksBtn" GroupName="SidebarNav" Content="Configure System" Style="{DynamicResource SidebarNavStyle}"/>
                            <!-- Renamed Navigation -->
                            <RadioButton x:Name="NavExecuteBtn" GroupName="SidebarNav" Content="Execution Settings" Style="{DynamicResource SidebarNavStyle}"/>
                        </StackPanel>

                        <!-- Secondary Actions (Moved from old Menu) -->
                        <StackPanel Grid.Row="2">
                            <TextBlock Text="TOOLS" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource SidebarLabelColor}" Margin="4,0,0,12"/>
                            <Button x:Name="ImportConfigBtn" Content="Import Profile" Style="{StaticResource SidebarToolsButtonStyle}"/>
                            <Button x:Name="ExportConfigBtn" Content="Export Profile" Style="{StaticResource SidebarToolsButtonStyle}"/>
                            <Button x:Name="MenuDocumentation" Content="GitHub Repository" Style="{StaticResource SidebarToolsButtonStyle}"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Content Views Container -->
                <Grid Grid.Column="1" x:Name="MainViewContainer" Background="{DynamicResource ContentBgColor}" Margin="0,0,0,0">
                    
                    <!-- View 1: Home (Hero & Cards) -->
                    <Grid x:Name="HomeView" Visibility="Visible">
                        <StackPanel HorizontalAlignment="Left" VerticalAlignment="Center" MaxWidth="820" Margin="64,0,64,0">
                            
                            <!-- Hero Typography -->
                            <TextBlock FontSize="42" FontWeight="Bold" Foreground="{DynamicResource HeroTitleColor}" Margin="0,0,0,16" TextAlignment="Left">
                                <Run Text="Windows Healing System"/>
                                <Run Text="  MADE WITH " FontSize="14" Foreground="{DynamicResource HeroSubtitleColor}" FontWeight="SemiBold"/>
                                <Run Text="&#xEB51;" FontFamily="Segoe Fluent Icons" FontSize="14" Foreground="White"/>
                                <Run Text=" BY PRAJWAL" FontSize="14" Foreground="{DynamicResource HeroSubtitleColor}" FontWeight="SemiBold"/>
                            </TextBlock>
                            <TextBlock Text="WHS is an open-source utility for configuring your Windows environment. It is open source, detailed, and carefully tested." 
                                       TextWrapping="Wrap" Foreground="{DynamicResource HeroSubtitleColor}" FontSize="16" LineHeight="24" Margin="0,0,0,40" MaxWidth="600" HorizontalAlignment="Left" TextAlignment="Left"/>
                            
                            <!-- Feature Actions -->
                            <StackPanel Orientation="Vertical">

                                <!-- Row 1: Default Selections -->
                                <Border BorderBrush="{DynamicResource BorderColor}" BorderThickness="1" CornerRadius="10" Padding="20,16" Margin="0,0,0,12">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0"
                                               Text="&#xE81C;"
                                               FontFamily="Segoe Fluent Icons"
                                               FontSize="32"
                                               Foreground="{DynamicResource OptionAccentColor}"
                                               VerticalAlignment="Center"
                                               Margin="0,0,20,0"/>

                                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="0,0,20,0">
                                        <TextBlock Text="Default Selections" FontSize="15" FontWeight="SemiBold"
                                                   Foreground="{DynamicResource HeroTitleColor}" Margin="0,0,0,4"/>
                                        <TextBlock Text="Our highly researched and curated selections for normal Windows users."
                                                   FontSize="13" Foreground="{DynamicResource HeroSubtitleColor}"
                                                   TextWrapping="Wrap" LineHeight="18"/>
                                    </StackPanel>

                                    <Button x:Name="HomeDefaultModeBtn" Grid.Column="2" VerticalAlignment="Center"
                                            BorderThickness="0" Padding="18,10" Cursor="Hand" Background="Transparent">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <Grid>
                                                    <Border x:Name="Bd" CornerRadius="6" Background="#4ADE80" Opacity="0.1"/>
                                                    <Border x:Name="HoverOverlay" CornerRadius="6" Background="Black" Opacity="0"/>
                                                    <StackPanel Margin="{TemplateBinding Padding}" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                                        <TextBlock Text="Apply Default" Foreground="#4ADE80" FontWeight="SemiBold" FontSize="13" Margin="0,0,7,0" VerticalAlignment="Center"/>
                                                        <TextBlock Text="&#xE76C;" FontFamily="Segoe Fluent Icons" FontSize="10" Foreground="#4ADE80" VerticalAlignment="Center"/>
                                                    </StackPanel>
                                                </Grid>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="Bd" Property="Opacity" Value="0.2"/>
                                                    </Trigger>
                                                    <Trigger Property="IsPressed" Value="True">
                                                        <Setter TargetName="HoverOverlay" Property="Opacity" Value="0.05"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Button.Template>
                                    </Button>
                                </Grid>
                                </Border>

                                <!-- Row 2: Custom Selections -->
                                <Border BorderBrush="{DynamicResource BorderColor}" BorderThickness="1" CornerRadius="10" Padding="20,16" Margin="0,0,0,0">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0"
                                               Text="&#xE713;"
                                               FontFamily="Segoe Fluent Icons"
                                               FontSize="32"
                                               Foreground="{DynamicResource WarningColor}"
                                               VerticalAlignment="Center"
                                               Margin="0,0,20,0"/>

                                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="0,0,20,0">
                                        <TextBlock Text="Custom Selections" FontSize="15" FontWeight="SemiBold"
                                                   Foreground="{DynamicResource HeroTitleColor}" Margin="0,0,0,4"/>
                                        <TextBlock Text="See and customize your own selections and export them for future use."
                                                   FontSize="13" Foreground="{DynamicResource HeroSubtitleColor}"
                                                   TextWrapping="Wrap" LineHeight="18"/>
                                    </StackPanel>

                                    <!-- Configure button -->
                                    <Button x:Name="HomeStartBtn" Grid.Column="2" VerticalAlignment="Center"
                                            BorderThickness="0" Padding="18,10" Cursor="Hand" Background="Transparent">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <Grid>
                                                    <Border x:Name="Bd" CornerRadius="6" Background="{DynamicResource SelectionAccentColor}" Opacity="0.1"/>
                                                    <Border x:Name="HoverOverlay" CornerRadius="6" Background="Black" Opacity="0"/>
                                                    <StackPanel Margin="{TemplateBinding Padding}" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                                        <TextBlock Text="Configure" Foreground="{DynamicResource SelectionAccentColor}" FontWeight="SemiBold" FontSize="13" Margin="0,0,7,0" VerticalAlignment="Center"/>
                                                        <TextBlock Text="&#xE76C;" FontFamily="Segoe Fluent Icons" FontSize="10" Foreground="{DynamicResource SelectionAccentColor}" VerticalAlignment="Center"/>
                                                    </StackPanel>
                                                </Grid>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="Bd" Property="Opacity" Value="0.2"/>
                                                    </Trigger>
                                                    <Trigger Property="IsPressed" Value="True">
                                                        <Setter TargetName="HoverOverlay" Property="Opacity" Value="0.05"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Button.Template>
                                    </Button>
                                </Grid>
                                </Border>

                            </StackPanel>
                        </StackPanel>
                    </Grid>

                    <!-- View 2: App Removal -->
                    <Grid x:Name="AppsView" Visibility="Collapsed">
                        <DockPanel>
                            <Border DockPanel.Dock="Top" Padding="16,12,16,0">
                                <StackPanel>
                                    <TextBlock Text="App Removal" FontWeight="SemiBold" FontSize="28" Margin="0,0,0,6" Foreground="{DynamicResource FgColor}"/>
                                    <TextBlock Text="Select which applications to strip from the Windows image." FontSize="15" Opacity="0.5" Margin="0,0,0,16" Foreground="{DynamicResource FgColor}" TextWrapping="Wrap"/>
                                    <Separator Background="{DynamicResource BorderColor}" Margin="0,0,0,24" Height="1" HorizontalAlignment="Stretch"/>

                                    <!-- Filter Bar row 1: preset chips + clear -->
                                    <WrapPanel Margin="0,0,0,10">
                                        <CheckBox x:Name="PresetDefaultApps" Content="Default" IsThreeState="True" ToolTip="Select the apps that are safe to remove for most users" Style="{DynamicResource PresetChipStyle}" AutomationProperties.Name="Default selection"/>
                                        <CheckBox x:Name="PresetLastUsed" Content="Last Used" IsThreeState="True" ToolTip="Select the apps that were removed the last time Windows Healing System was run" Style="{DynamicResource PresetChipStyle}" AutomationProperties.Name="Last used selection"/>
                                        <!-- Dynamic JSON presets inserted here at runtime -->
                                        <StackPanel x:Name="JsonPresetsPanel" Orientation="Horizontal"/>
                                        <!-- Spacer before Clear -->
                                        <Separator Width="1" Height="20" Background="{DynamicResource BorderColor}" VerticalAlignment="Center" Margin="8,0,4,0"/>
                                        <Button x:Name="ClearAppSelectionBtn" ToolTip="Clear all selected apps" Style="{DynamicResource GhostButtonStyle}" AutomationProperties.Name="Clear Selection" Height="32" Padding="8,4">
                                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                <TextBlock Text="&#xE711;" FontFamily="Segoe Fluent Icons" FontSize="12" VerticalAlignment="Center" Margin="0,0,6,0" Foreground="{DynamicResource FgColor}"/>
                                                <TextBlock Text="Clear Selection" FontWeight="SemiBold" FontSize="13" VerticalAlignment="Center" Foreground="{DynamicResource FgColor}"/>
                                            </StackPanel>
                                        </Button>
                                    </WrapPanel>

                                    <!-- Filter Bar row 2: only installed + search -->
                                    <Grid Margin="0,0,0,12">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <CheckBox x:Name="OnlyInstalledAppsBox" Grid.Column="0" Content="Only show installed apps" IsChecked="False" Foreground="{DynamicResource FgColor}" VerticalAlignment="Center" AutomationProperties.Name="Only show installed apps"/>

                                        <Border x:Name="AppSearchBorder" Grid.Column="1">
                                            <Border.Style>
                                                <Style TargetType="Border" BasedOn="{StaticResource SearchBoxBorderStyle}">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding ElementName=AppSearchBox, Path=IsFocused}" Value="True">
                                                            <Setter Property="Background" Value="{DynamicResource InputFocusColor}"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Grid.Column="0" Text="&#xE721;" FontFamily="Segoe Fluent Icons" FontSize="14" VerticalAlignment="Center" Margin="10,0,8,0" Foreground="{DynamicResource FgColor}"/>
                                                <TextBlock x:Name="AppSearchPlaceholder" Grid.Column="1" Text="Search app" Foreground="{DynamicResource FgColor}" Opacity="0.5" FontSize="13" Margin="0,0,0,1" VerticalAlignment="Center" IsHitTestVisible="False"/>
                                                <TextBox x:Name="AppSearchBox" Grid.Column="1" Background="Transparent" Foreground="{DynamicResource FgColor}" BorderThickness="0" FontSize="13" Margin="0,0,0,1" VerticalAlignment="Center" Text="" AutomationProperties.Name="Search app"/>
                                            </Grid>
                                        </Border>
                                    </Grid>
                                </StackPanel>
                            </Border>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                
                                <!-- Apps List -->
                                <Grid Grid.Row="0" Margin="24,0,24,12">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>
                                    <!-- Apps content -->
                                    <Border Background="Transparent" BorderThickness="0" Grid.Row="0" Padding="0">
                                        <Grid>
                                            <ScrollViewer x:Name="AppSelectionPanel" VerticalScrollBarVisibility="Auto">
                                                <StackPanel x:Name="AppCategoryContainer" Margin="0,0,0,0"/>
                                            </ScrollViewer>
                                            <Border x:Name="LoadingAppsIndicator" CornerRadius="0,0,8,8" Background="{DynamicResource CardBgColor}" Opacity="0.85" Visibility="Collapsed">
                                                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                                    <TextBlock Text="&#xF16A;" FontFamily="Segoe Fluent Icons" FontSize="28" Foreground="{DynamicResource FgColor}" HorizontalAlignment="Center" Margin="0,0,0,8" RenderTransformOrigin="0.5,0.5">
                                                        <TextBlock.RenderTransform>
                                                            <RotateTransform Angle="0"/>
                                                        </TextBlock.RenderTransform>
                                                        <TextBlock.Triggers>
                                                            <EventTrigger RoutedEvent="Loaded">
                                                                <BeginStoryboard>
                                                                    <Storyboard>
                                                                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                                                                                         From="0" To="360"
                                                                                         Duration="0:0:1.5"
                                                                                         RepeatBehavior="Forever"/>
                                                                    </Storyboard>
                                                                </BeginStoryboard>
                                                            </EventTrigger>
                                                        </TextBlock.Triggers>
                                                    </TextBlock>
                                                    <TextBlock Text="Loading apps..." FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource FgColor}" HorizontalAlignment="Center"/>
                                                </StackPanel>
                                            </Border>
                                        </Grid>
                                    </Border>
                                </Grid>

                                <!-- Status Info -->
                                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="24,0,24,0">
                                    <TextBlock x:Name="AppSelectionStatus" Text="" Foreground="{DynamicResource FgColor}" Opacity="0.7" FontSize="13" Margin="4,0,0,8" HorizontalAlignment="Left"/>
                                </StackPanel>
                            </Grid>
                        </DockPanel>
                    </Grid>

                    <!-- View 3: Tweaks -->
                    <Grid x:Name="TweaksView" Visibility="Collapsed">
                        <DockPanel>
                            <Border DockPanel.Dock="Top" Padding="16,12,16,0" Background="{DynamicResource ContentBgColor}">
                                <StackPanel>
                                    <TextBlock Text="Configure System" FontWeight="SemiBold" FontSize="28" Margin="0,0,0,6" Foreground="{DynamicResource FgColor}"/>
                                    <TextBlock Text="Configure services, features, settings and Windows updates." FontSize="15" Opacity="0.5" Margin="0,0,0,16" Foreground="{DynamicResource FgColor}" TextWrapping="Wrap"/>
                                    <Separator Background="{DynamicResource BorderColor}" Margin="0,0,0,24" Height="1" HorizontalAlignment="Stretch"/>
                                
                                    <!-- Consolidated Filter Bar -->
                                    <DockPanel Margin="0,0,0,16" LastChildFill="False">
                                        <!-- Search and View Toggles on Right -->
                                        <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                                            <!-- View Toggles -->
                                            <StackPanel Orientation="Horizontal" Margin="0,0,12,0" VerticalAlignment="Center">
                                                <RadioButton x:Name="ViewModeListBtn" Style="{DynamicResource SegmentedButtonStyle}" GroupName="TweaksViewMode" IsChecked="True" ToolTip="List View" Height="32" Width="36" Padding="0">
                                                    <TextBlock Text="&#xE8FD;" FontFamily="Segoe Fluent Icons" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </RadioButton>
                                                <RadioButton x:Name="ViewModeMatrixBtn" Style="{DynamicResource SegmentedButtonStyle}" GroupName="TweaksViewMode" ToolTip="Grid View" Height="32" Width="36" Padding="0">
                                                    <TextBlock Text="&#xF0E2;" FontFamily="Segoe Fluent Icons" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </RadioButton>
                                            </StackPanel>

                                            <!-- Search -->
                                            <Border x:Name="TweakSearchBorder" Width="200" VerticalAlignment="Center">
                                                <Border.Style>
                                                    <Style TargetType="Border" BasedOn="{StaticResource SearchBoxBorderStyle}">
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding ElementName=TweakSearchBox, Path=IsFocused}" Value="True">
                                                                <Setter Property="Background" Value="{DynamicResource InputFocusColor}"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Grid.Column="0" Text="&#xE721;" FontFamily="Segoe Fluent Icons" FontSize="14" VerticalAlignment="Center" Margin="10,0,8,0" Foreground="{DynamicResource FgColor}"/>
                                                    <TextBlock x:Name="TweakSearchPlaceholder" Grid.Column="1" Text="Search settings" Foreground="{DynamicResource FgColor}" Opacity="0.5" FontSize="13" Margin="0,0,0,1" VerticalAlignment="Center" IsHitTestVisible="False"/>
                                                    <TextBox x:Name="TweakSearchBox" Grid.Column="1" Background="Transparent" Foreground="{DynamicResource FgColor}" BorderThickness="0" FontSize="13" Margin="0,0,0,1" VerticalAlignment="Center" Text=""/>
                                                </Grid>
                                            </Border>
                                        </StackPanel>

                                        <!-- Preset Chips on Left -->
                                        <WrapPanel VerticalAlignment="Center" Margin="0,0,16,0">
                                            <CheckBox x:Name="PresetDefaultTweaksBtn" IsThreeState="True" Content="Default settings" ToolTip="Select the settings that are recommended for most people" Style="{DynamicResource PresetChipStyle}" AutomationProperties.Name="Default settings"/>
                                            <CheckBox x:Name="PresetLastUsedTweaksBtn" IsThreeState="True" Content="Last used settings" ToolTip="Select the settings that were used the last time Windows Healing System was run" Style="{DynamicResource PresetChipStyle}" AutomationProperties.Name="Last used settings"/>
                                            <CheckBox x:Name="PresetPrivacyTweaksBtn" IsThreeState="True" Content="Privacy &amp; Suggested Content" ToolTip="Select all Privacy &amp; Suggested Content tweaks" Style="{DynamicResource PresetChipStyle}" AutomationProperties.Name="All Privacy and Suggested Content"/>
                                            <CheckBox x:Name="PresetAITweaksBtn" IsThreeState="True" Content="AI features" ToolTip="Select all AI feature tweaks" Style="{DynamicResource PresetChipStyle}" AutomationProperties.Name="All AI features"/>
                                            <Button x:Name="ClearAllTweaksBtn" ToolTip="Clear all selected tweaks" Style="{DynamicResource GhostButtonStyle}" AutomationProperties.Name="Clear Selection" Height="32" Padding="8,4" Margin="4,0,0,0">
                                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                    <TextBlock Text="&#xE711;" FontFamily="Segoe Fluent Icons" FontSize="12" VerticalAlignment="Center" Margin="0,0,6,0" Foreground="{DynamicResource FgColor}"/>
                                                    <TextBlock Text="Clear Selection" FontWeight="SemiBold" FontSize="13" VerticalAlignment="Center" Foreground="{DynamicResource FgColor}"/>
                                                </StackPanel>
                                            </Button>
                                        </WrapPanel>
                                    </DockPanel>
                                </StackPanel>
                            </Border>
                            <Grid>
                                <ScrollViewer x:Name="TweaksScrollViewer" VerticalScrollBarVisibility="Auto" Margin="0,-16,0,0" Padding="16,12,16,0">
                                    <!-- 2-column matrix: 2 sections visible at a time, scroll for more -->
                                    <Grid x:Name="TweaksGrid" Margin="0,12,0,8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition x:Name="TweaksCol0Def" Width="*"/>
                                            <ColumnDefinition x:Name="TweaksCol1Def" Width="0"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- Left column: Dynamically populated -->
                                        <StackPanel Grid.Column="0" x:Name="Column0Panel" Margin="0,0,12,0"/>

                                        <!-- Right column: Dynamically populated -->
                                        <StackPanel Grid.Column="1" x:Name="Column1Panel" Margin="12,0,0,0"/>
                                    </Grid>
                                </ScrollViewer>
                                <!-- Top fade gradient -->
                                <Border IsHitTestVisible="False" VerticalAlignment="Top" Height="10" Margin="0,-12,20,0" Background="{DynamicResource ContentBgColor}">
                                    <Border.OpacityMask>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                            <GradientStop Color="Black" Offset="0"/>
                                            <GradientStop Color="Black" Offset="0.5"/>
                                            <GradientStop Color="Transparent" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Border.OpacityMask>
                                </Border>
                                <!-- Bottom fade gradient -->
                                <Border IsHitTestVisible="False" VerticalAlignment="Bottom" Height="20" Margin="0,0,20,0" Background="{DynamicResource ContentBgColor}">
                                    <Border.OpacityMask>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                            <GradientStop Color="Transparent" Offset="0"/>
                                            <GradientStop Color="Black" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Border.OpacityMask>
                                </Border>
                            </Grid>
                        </DockPanel>
                    </Grid>

                    <!-- View 4: Execution Settings -->
                    <Grid x:Name="DeployView" Visibility="Collapsed">
                        <DockPanel>
                            <!-- Page Header -->
                            <Border DockPanel.Dock="Top" Padding="24,12,24,0">
                                <StackPanel>
                                    <TextBlock Text="Execution Settings" FontWeight="SemiBold" FontSize="28" Margin="0,0,0,6" Foreground="{DynamicResource FgColor}"/>
                                    <TextBlock Text="Review pending executions and configure application targets." FontSize="15" Opacity="0.5" Margin="0,0,0,16" Foreground="{DynamicResource FgColor}" TextWrapping="Wrap"/>
                                    <Separator Background="{DynamicResource BorderColor}" Margin="0,0,0,0" Height="1" HorizontalAlignment="Stretch"/>
                                </StackPanel>
                            </Border>

                            <!-- Two-column body -->
                            <Grid Margin="24,20,24,20">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>   <!-- Left: settings -->
                                    <ColumnDefinition Width="32"/>  <!-- gap -->
                                    <ColumnDefinition Width="340"/> <!-- Right: overview -->
                                </Grid.ColumnDefinitions>

                                <!-- Ã¢â€¢ÂÃ¢â€¢ÂÃ¢â€¢Â LEFT COLUMN: flat sections Ã¢â€¢ÂÃ¢â€¢ÂÃ¢â€¢Â -->
                                <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto">
                                    <StackPanel>

                                        <!-- Ã¢â€â‚¬Ã¢â€â‚¬ Section: Apply Changes To Ã¢â€â‚¬Ã¢â€â‚¬ -->
                                        <TextBlock Text="Apply Changes To" FontWeight="SemiBold" FontSize="15" Foreground="{DynamicResource FgColor}" Margin="0,0,0,8"/>
                                        <WrapPanel x:Name="UserSelectionPanel" Margin="0,0,0,8">
                                            <RadioButton x:Name="UserSelectionChip0" GroupName="UserSelectionGroup" Content="Current User" IsChecked="True" Style="{DynamicResource SelectionChipStyle}" AutomationProperties.Name="Current User"/>
                                            <RadioButton x:Name="UserSelectionChip1"   GroupName="UserSelectionGroup" Content="Other User"    Style="{DynamicResource SelectionChipStyle}" AutomationProperties.Name="Other User"/>
                                            <RadioButton x:Name="UserSelectionChip2" GroupName="UserSelectionGroup" Content="Default User (Sysprep)" Style="{DynamicResource SelectionChipStyle}" AutomationProperties.Name="Default User Sysprep"/>
                                        </WrapPanel>
                                        <StackPanel x:Name="OtherUserPanel" Visibility="Collapsed" Margin="0,6,0,6">
                                            <Border IsEnabled="{Binding ElementName=OtherUsernameTextBox, Path=IsEnabled}">
                                                <Border.Style>
                                                    <Style TargetType="Border" BasedOn="{StaticResource UserTextBoxBorderStyle}">
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding ElementName=OtherUsernameTextBox, Path=IsFocused}" Value="True">
                                                                <Setter Property="Background" Value="{DynamicResource InputFocusColor}"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Grid.Column="0" Text="&#xE77B;" FontFamily="Segoe Fluent Icons" FontSize="14" VerticalAlignment="Center" Margin="0,0,8,0" Foreground="{DynamicResource FgColor}" Opacity="0.6"/>
                                                    <TextBlock x:Name="UsernameTextBoxPlaceholder" Grid.Column="1" Text="Enter username" Foreground="{DynamicResource FgColor}" Opacity="0.4" FontSize="13" VerticalAlignment="Center" IsHitTestVisible="False"/>
                                                    <TextBox x:Name="OtherUsernameTextBox" Grid.Column="1" Style="{DynamicResource UserTextBoxStyle}" Text="" AutomationProperties.Name="Enter username"/>
                                                </Grid>
                                            </Border>
                                            <TextBlock x:Name="UsernameValidationMessage" Text="" FontStyle="Italic" Foreground="{DynamicResource CloseHover}" FontSize="11" Margin="0,6,0,0" TextWrapping="Wrap"/>
                                        </StackPanel>
                                        <TextBlock x:Name="UserSelectionDescription" Text="Changes will be applied to the currently logged-in user profile." Foreground="{DynamicResource FgColor}" Opacity="0.6" FontSize="13" TextWrapping="Wrap" Margin="0,4,0,0"/>

                                        <!-- Divider -->
                                        <Separator Background="{DynamicResource BorderColor}" Height="1" Margin="0,20,0,20" HorizontalAlignment="Stretch" Opacity="0.5"/>

                                        <!-- Ã¢â€â‚¬Ã¢â€â‚¬ Section: Remove Apps For Ã¢â€â‚¬Ã¢â€â‚¬ -->
                                        <Border x:Name="AppRemovalScopeSection">
                                            <StackPanel>
                                                <TextBlock Text="Remove Apps For" FontWeight="SemiBold" FontSize="15" Foreground="{DynamicResource FgColor}" Margin="0,0,0,8"/>
                                                <WrapPanel x:Name="AppRemovalScopePanel" Margin="0,0,0,8">
                                                    <RadioButton x:Name="AppRemovalScopeChip0"    GroupName="AppScopeGroup" Content="All Users"          IsChecked="True" Style="{DynamicResource SelectionChipStyle}" AutomationProperties.Name="All Users"/>
                                                    <RadioButton x:Name="AppRemovalScopeChip1" GroupName="AppScopeGroup" Content="Current User Only"  Style="{DynamicResource SelectionChipStyle}" AutomationProperties.Name="Current User Only"/>
                                                    <RadioButton x:Name="AppRemovalScopeChip2"  GroupName="AppScopeGroup" Content="Target User Only"   Style="{DynamicResource SelectionChipStyle}" Visibility="Collapsed" AutomationProperties.Name="Target User Only"/>
                                                </WrapPanel>
                                                <TextBlock x:Name="AppRemovalScopeDescription" Text="Apps will be removed for all users and from the Windows image to prevent reinstallation for new users." Foreground="{DynamicResource FgColor}" Opacity="0.6" FontSize="13" TextWrapping="Wrap" Margin="2,2,0,0"/>
                                            </StackPanel>
                                        </Border>

                                        <!-- Divider -->
                                        <Separator Background="{DynamicResource BorderColor}" Height="1" Margin="0,20,0,20" HorizontalAlignment="Stretch" Opacity="0.5"/>

                                        <!-- Ã¢â€â‚¬Ã¢â€â‚¬ Section: Options Ã¢â€â‚¬Ã¢â€â‚¬ -->
                                        <TextBlock Text="Options" FontWeight="SemiBold" FontSize="15" Foreground="{DynamicResource FgColor}" Margin="0,0,0,10"/>
                                        <CheckBox x:Name="RestorePointCheckBox" Style="{DynamicResource FeatureCheckboxStyle}" IsChecked="True" Content="Create a system restore point (Recommended)" AutomationProperties.Name="Create a system restore point (Recommended)" Margin="0,0,0,4"/>
                                        <CheckBox x:Name="RestartExplorerCheckBox" Style="{DynamicResource FeatureCheckboxStyle}" Content="Restart the Windows Explorer process to apply all changes immediately" AutomationProperties.Name="Restart the Windows Explorer process to apply all changes immediately"/>

                                    </StackPanel>
                                </ScrollViewer>

                                <!-- Ã¢â€¢ÂÃ¢â€¢ÂÃ¢â€¢Â RIGHT COLUMN: Pending Executions Ã¢â€¢ÂÃ¢â€¢ÂÃ¢â€¢Â -->
                                <Grid Grid.Column="2">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>  <!-- header -->
                                        <RowDefinition Height="*"/>     <!-- list -->
                                        <RowDefinition Height="Auto"/>  <!-- apply button -->
                                    </Grid.RowDefinitions>

                                    <!-- Header row with subtle bottom border -->
                                    <StackPanel Grid.Row="0" Margin="0,0,0,12">
                                        <TextBlock Text="Pending Changes" FontWeight="SemiBold" FontSize="15" Foreground="{DynamicResource FgColor}" Margin="0,0,0,10"/>
                                        <Separator Background="{DynamicResource BorderColor}" Height="1" HorizontalAlignment="Stretch" Opacity="0.5"/>
                                    </StackPanel>

                                    <!-- Scrollable list of pending items -->
                                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Margin="0,0,0,16">
                                        <StackPanel x:Name="InlineOverviewPanel">
                                            <!-- Injected at runtime by UpdateInlineOverview -->
                                            <TextBlock Text="No changes selected." Foreground="{DynamicResource FgColor}" Opacity="0.5" FontStyle="Italic" FontSize="13"/>
                                        </StackPanel>
                                    </ScrollViewer>

                                    <!-- Apply Changes — green accent button -->
                                    <Button x:Name="DeploymentApplyBtn" Grid.Row="2" Height="48" HorizontalAlignment="Stretch" Cursor="Hand" AutomationProperties.Name="Apply Changes">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <Grid>
                                                    <Border x:Name="Bd" CornerRadius="8" Background="{DynamicResource OptionAccentColor}"/>
                                                    <Border x:Name="HoverOverlay" CornerRadius="8" Background="Black" Opacity="0"/>
                                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                                        <TextBlock Text="&#xE73E;" FontFamily="Segoe Fluent Icons" FontSize="18" VerticalAlignment="Center" Margin="0,0,8,0" Foreground="White"/>
                                                        <TextBlock Text="Apply Changes" VerticalAlignment="Center" FontSize="15" FontWeight="SemiBold" Margin="0,0,0,1" Foreground="White"/>
                                                    </StackPanel>
                                                </Grid>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsEnabled" Value="False">
                                                        <Setter TargetName="Bd" Property="Opacity" Value="0.5"/>
                                                    </Trigger>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="HoverOverlay" Property="Opacity" Value="0.15"/>
                                                    </Trigger>
                                                    <Trigger Property="IsPressed" Value="True">
                                                        <Setter TargetName="HoverOverlay" Property="Opacity" Value="0.25"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Button.Template>
                                    </Button>
                                </Grid>

                            </Grid>
                        </DockPanel>
                    </Grid>
                </Grid>
            </Grid>

            <!-- Modal Overlay for MessageBoxes -->
            <Rectangle x:Name="ModalOverlay" 
                       Grid.RowSpan="2"
                       Fill="#80000000" 
                       Visibility="Collapsed" 
                       Panel.ZIndex="1000"
                       RadiusX="8"
                       RadiusY="8"
                       Margin="-1"/>
        </Grid>
    </Border>
</Window>





