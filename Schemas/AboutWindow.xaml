<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="About Windows Healing System" 
        Width="500"
        SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="False"
        ShowInTaskbar="False">

    <Border BorderBrush="{DynamicResource BorderColor}" 
            BorderThickness="1" 
            CornerRadius="12"
            Background="{DynamicResource CardBgColor}"
            Margin="25">
        <Border.Effect>
            <DropShadowEffect Color="Black" 
                            Opacity="0.3" 
                            BlurRadius="30" 
                            ShadowDepth="0" 
                            Direction="0"/>
        </Border.Effect>
        
        <Grid Margin="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Title Bar -->
            <Grid Grid.Row="0" x:Name="TitleBar" Height="48" Background="Transparent">
                <TextBlock Text="About Windows Healing System" 
                           Foreground="{DynamicResource FgColor}"
                           FontSize="20"
                           FontWeight="SemiBold"
                           VerticalAlignment="Center" 
                           Margin="24,12,0,0"/>
            </Grid>

            <!-- Message Content -->
            <Grid Grid.Row="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Message Text -->
                <Grid Grid.Row="0" Margin="24,16,24,24">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Version -->
                    <TextBlock Grid.Row="0" Grid.Column="0"
                            Text="Version:" 
                            FontSize="14"
                            Opacity="0.6"
                            Foreground="{DynamicResource FgColor}"
                            Margin="0,0,16,8"/>
                    <TextBlock x:Name="VersionText"
                            Grid.Row="0" Grid.Column="1"
                            Text="0.0.0" 
                            FontSize="14"
                            Foreground="{DynamicResource FgColor}"
                            Margin="0,0,0,8"/>
                    
                    <!-- Author -->
                    <TextBlock Grid.Row="1" Grid.Column="0"
                            Text="Author:" 
                            FontSize="14"
                            Opacity="0.6"
                            Foreground="{DynamicResource FgColor}"
                            Margin="0,0,16,8"/>
                    <TextBlock Grid.Row="1" Grid.Column="1"
                            Text="Raphire" 
                            FontSize="14"
                            Foreground="{DynamicResource FgColor}"
                            Margin="0,0,0,8"/>
                    
                    <!-- Project Link -->
                    <TextBlock Grid.Row="2" Grid.Column="0"
                            Text="Project:" 
                            FontSize="14"
                            Opacity="0.6"
                            Foreground="{DynamicResource FgColor}"
                            Margin="0,0,16,0"/>
                    <TextBlock x:Name="ProjectLink"
                            Grid.Row="2" Grid.Column="1"
                            Text="https://github.com" 
                            FontSize="14"
                            Style="{DynamicResource HyperlinkStyle}"
                            Margin="0,0,0,0"/>
                </Grid>

                <!-- Separator -->
                <Border Grid.Row="1" 
                        Height="1" 
                        Background="{DynamicResource BorderColor}"
                        Margin="0,0"/>

                <!-- Content -->
                <StackPanel Grid.Row="2" Margin="24,24">
                    <!-- Donation Message -->
                    <TextBlock Text="Windows Healing System is a passion project that I maintain in my free time. If you've found this tool useful, please consider making a small donation to support its development. I really appreciate it!"
                            FontSize="14"
                            Foreground="{DynamicResource FgColor}"
                            TextWrapping="Wrap"
                            LineHeight="22"
                            Opacity="0.85"
                            Margin="0,0,0,15"/>

                    <!-- Ko-fi Link -->
                    <Grid HorizontalAlignment="Left">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock x:Name="KofiLinkIcon"
                                Grid.Column="0"
                                FontSize="16"
                                Style="{DynamicResource HyperlinkStyle}"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                FontWeight="SemiBold"
                                FontFamily="Segoe Fluent Icons"
                                Text="&#xEB52;" 
                                Foreground="{DynamicResource CloseHover}"
                                Margin="0,0,8,0"/>
                        
                        <TextBlock x:Name="KofiLink"
                                Grid.Column="1"
                                FontSize="16"
                                Style="{DynamicResource HyperlinkStyle}"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                FontWeight="Medium"
                                Text="Support me on Ko-fi"/>
                    </Grid>
                </StackPanel>
            </Grid>

            <!-- Button Panel -->
            <Border Grid.Row="2" 
                    Background="{DynamicResource BgColor}"
                    BorderBrush="{DynamicResource BorderColor}"
                    BorderThickness="0,1,0,0"
                    Padding="16,16"
                    CornerRadius="0,0,12,12">
                <StackPanel x:Name="ButtonPanel"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right">
                    <Button x:Name="CloseButton" 
                            Content="Close" 
                            Height="36" MinWidth="90" Margin="4,0"
                            Style="{DynamicResource SecondaryButtonStyle}"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
